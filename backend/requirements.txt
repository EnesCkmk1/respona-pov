# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4

# HTTP + realtime (used when wiring real providers)
httpx>=0.27
websockets>=13.0

# Database (optional at runtime — only needed when SUPABASE_* is set)
supabase>=2.6

# After installing, pin exact versions for reproducible/secure builds:
#   pip freeze > requirements.lock.txt
